apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kubernetes-volume-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: kubernetes-volume1
